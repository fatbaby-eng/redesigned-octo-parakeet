{
  "name": "tb-atlas",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "schema:remote": "wrangler d1 execute tb-atlas --remote --file=./schema.sql",
    "schema:local": "wrangler d1 execute tb-atlas --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
